# shared

Single source of truth for cross-app TypeScript types (User, Role, Shift, Reception, SellerStockPallet, Task, …) and the generated API client, so mobile, web-dashboard, and backend never drift out of sync on data shapes.

## Status

Skeleton only. Not yet scaffolded — see [../../docs/architecture.md](../../docs/architecture.md).
