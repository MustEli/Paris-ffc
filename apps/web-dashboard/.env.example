# Points the web app at the backend API. For local dev against a
# locally-running backend, leave this unset (defaults to
# http://localhost:3000). For a real deployment, set it to the live
# backend URL, e.g. https://warehouse-hq-backend.onrender.com
VITE_API_BASE_URL=
